{
  "platform": "ios",
  "currentVersion": "0.1.10",
  "_readme": [
    "판올림마다 이 파일에 한 칸을 더한다. 비어 있으면 MCP 가 '변경 없음' 이라고 답할 근거가 없어",
    "에이전트가 추측으로 코드를 고치게 된다 — 그게 이 파일이 존재하는 이유다.",
    "changes[] 의 각 항목은 '무엇이 어떻게 바뀌었나' 가 아니라 '코드를 어떻게 고치나' 로 쓴다.",
    "읽는 쪽이 사람이 아니라 코딩 에이전트이고, 그 결과가 고객사 코드에 그대로 들어간다."
  ],
  "migrations": [
    {
      "from": "0.1.0",
      "to": "0.1.1",
      "breaking": false,
      "summary": "공개 API 변경 없음. 연동 스니펫(Snippets/ios.json)이 태그에 실린 것이 이 판올림의 내용이다.",
      "action": "의존성 해석만 다시 하면 된다. 코드는 고칠 것이 없다.",
      "changes": []
    },
    {
      "from": "0.1.1",
      "to": "0.1.2",
      "breaking": false,
      "summary": "공개 API 변경 없음. 마이그레이션 지침(이 파일)과 CHANGELOG 가 태그에 실렸다.",
      "action": "의존성 해석만 다시 하면 된다. 코드는 고칠 것이 없다.",
      "changes": []
    },
    {
      "from": "0.1.2",
      "to": "0.1.3",
      "breaking": false,
      "summary": "로케이터 수신 진단이 로그로 남기 시작한다. 측위 시작 7초 뒤 한 번, 등록한 로케이터 중 신호가 안 잡히는 것이 있으면 E4003(WARN)을, 신호는 충분한데 좌표가 안 나오면 E4002(ERROR)를 남긴다.",
      "action": "코드는 고칠 것이 없다. 다만 로그에 E4003 이 새로 보일 수 있는데, 이는 고장이 아니라 유지보수 신호다 — 마스터가 살아 있으면 측위는 계속되고 감도만 떨어진다.",
      "changes": []
    },
    {
      "from": "0.1.3",
      "to": "0.1.4",
      "breaking": false,
      "summary": "서버 원격 설정(remote_config)에 문자열이 아닌 값이 섞여 있어도 초기화가 실패하지 않는다. 0.1.3 이하는 이 자루를 [String: String] 로 좁게 읽어, 서버가 정수·불리언 설정을 하나 켜는 순간 initialize 가 통째로 실패했다.",
      "action": "코드는 고칠 것이 없다. 0.1.3 이하를 쓰고 있고 initialize 가 \"응답 해석 실패(E5005)\" 로 떨어진다면 이 판올림이 해결책이다."
    },
    {
      "from": "0.1.4",
      "to": "0.1.5",
      "breaking": false,
      "summary": "SDK 저장소 주소가 바뀌었다: onecheck-inc/onesight-mobile-swift → onecheck-inc/OneS1ght-iOS-SDK. 코드 동작은 그대로다.",
      "action": "Package.swift 의 .package(url:) 를 새 주소로 바꾸고, .product(package:) 식별자도 \"OneS1ght-iOS-SDK\" 로 함께 바꾼다 — SPM 은 패키지 식별자를 저장소 이름에서 뽑으므로 URL 만 고치면 \"unknown package\" 로 빌드가 깨진다. Xcode 프로젝트라면 패키지 의존성을 지웠다가 새 주소로 다시 추가한다. 옛 주소는 GitHub 이 리다이렉트해 주지만 영구 보장은 아니다."
    },
    {
      "from": "0.1.5",
      "to": "0.1.6",
      "breaking": false,
      "summary": "프로필 발급 예제가 컴파일되지 않던 것을 고쳤다. 코드 동작은 그대로다.",
      "action": "스니펫대로 붙였다면 프로필 발급 부분이 컴파일되지 않았을 것이다. `savedProfileId ?? (try await ...)` 는 `??` 오른쪽이 autoclosure 라 try/await 를 담을 수 없다. if let 으로 풀어 쓴 새 스니펫으로 교체하라."
    },
    {
      "from": "0.1.6",
      "to": "0.1.7",
      "breaking": false,
      "summary": "GeoSpace 가 층 ID 를 숫자로 보낼 때 층 목록이 비던 것을 고쳤다. 연동 코드는 그대로다.",
      "action": "고칠 것 없다. 빌딩을 골라도 층 드롭다운이 비어 있었다면 이 판올림으로 해결된다 — SDK 안에서 디코딩이 실패하고 있었고, 호출부가 오류를 삼켜 빈 목록만 보였다."
    },
    {
      "from": "0.1.7",
      "to": "0.1.8",
      "breaking": false,
      "summary": "실시간 수신이 측위 시작 전에도 붙는다. 층을 지정하면 그 시점부터 콘솔 변경이 온다.",
      "action": "고칠 것 없다. onConfigChanged 를 걸어 두었는데 측위를 시작하기 전에는 신호가 오지 않았다면 이 판올림으로 해결된다. 층을 지정(setFloorMap)하는 순간부터 연결된다."
    },
    {
      "from": "0.1.8",
      "to": "0.1.9",
      "breaking": true,
      "summary": "initialize 가 더 이상 기기 미지원으로 실패하지 않는다. 측위 불가 기기에서도 도면·존을 받아올 수 있고, 차단은 begin() 한 곳으로 모였다. 실시간 변경 이벤트가 하나도 도착하지 않던 것과 층 목록이 도면을 통째로 받던 것도 함께 고쳤다.",
      "action": "initialize 의 성공을 '이 기기에서 측위 가능' 으로 읽고 있었다면 고쳐야 한다. initialize 는 이제 키·설정만 확인하고 osVersionTooLow(E2001)·deviceNotSupported(E2002) 를 던지지 않는다. 측위 가능 여부는 ① 미리 알고 싶으면 OneS1ght.deviceAvailability / isDeviceAvailable (initialize 전에도 호출 가능, throw 없음), ② 시작할 때 알아도 되면 FloorSession.begin() 의 throw 로 확인한다. 둘 다 이전부터 있던 API 다. initialize 의 catch 에서 E2001·E2002 를 다루던 분기가 있다면 그 분기는 이제 도달하지 않는다 — begin() 쪽으로 옮긴다. 그대로 두어도 앱이 깨지지는 않지만, 미지원 기기에서 '초기화 실패' 로 화면을 막고 있었다면 그 화면이 이제 잘못된 안내가 된다.",
      "changes": [
        {
          "why": "initialize 는 이제 기기 지원 여부를 보지 않는다. 측위 불가 기기에서도 도면·존을 받아야 하기 때문이다. E2001·E2002 를 여기서 잡던 분기는 도달하지 않는다.",
          "before": "do {\n    try await OneS1ght.initialize(sdkKey: key)\n} catch SdkError.deviceNotSupported {\n    showUnsupportedScreen()      // 여기로 오지 않는다\n} catch SdkError.osVersionTooLow {\n    showOsTooLowScreen()         // 여기로 오지 않는다\n} catch {\n    showError(error)\n}",
          "after": "// 기기 지원 여부는 초기화 전에 따로 묻는다 (throw 없음)\nswitch OneS1ght.deviceAvailability {\ncase .deviceNotSupported: markPositioningUnavailable()   // 화면은 계속 쓴다\ncase .osVersionTooLow:    markOsTooLow()\ncase .available:          break\n}\n\ndo {\n    try await OneS1ght.initialize(sdkKey: key)   // 키·설정만 확인한다\n} catch {\n    showError(error)\n}"
        },
        {
          "why": "미지원 기기라고 화면을 막지 않는다. 초기화가 통과하므로 건물·층·도면·존을 평소대로 받을 수 있고, 막아야 하는 것은 측위 시작뿐이다.",
          "before": "guard OneS1ght.isInitialized else { return }   // 미지원 기기에선 여기서 끝났다\nlet buildings = try await OneS1ght.buildings()",
          "after": "// 미지원 기기에서도 여기까지 온다 — 지도와 존은 그려도 된다\nlet buildings = try await OneS1ght.buildings()\nlet floor = try await OneS1ght.floor(buildingId, floorId)\nlet zones = try await OneS1ght.zones(buildingId, floorId)"
        },
        {
          "why": "차단은 begin() 한 곳으로 모였다. 측위 시작 실패를 여기서 잡아 사용자에게 알린다.",
          "before": "try await OneS1ght.floorSession().begin()   // 미지원은 initialize 에서 이미 걸러졌다고 가정",
          "after": "do {\n    try await OneS1ght.floorSession().begin()\n} catch SdkError.deviceNotSupported {\n    // 이 기기는 UWB 가 없다 — 측위만 끄고 나머지 화면은 그대로 둔다\n    markPositioningUnavailable()\n} catch SdkError.osVersionTooLow {\n    markOsTooLow()\n}"
        }
      ]
    },
    {
      "from": "0.1.9",
      "to": "0.1.10",
      "breaking": false,
      "summary": "중단됐다 돌아온 측위 세션이 다시 시작되지 않아, 좌표가 그 자리에서 멈추던 것을 고쳤다. 앱이 잠깐 백그라운드로 갔다 오는 것만으로도 일어나서 간헐적으로 보였다.",
      "action": "고칠 것 없다. 판올림만 하면 된다. 다만 이 증상을 앱에서 따로 우회하고 있었다면(예: 좌표가 멈추면 stop/start 를 다시 부르는 코드) 이제 필요 없다."
    }
  ]
}
